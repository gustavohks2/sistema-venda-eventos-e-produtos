const module = (function() {

   DOMElements = {
      // Elementos do modelo de objetos do documento - DOM
   }

   const init = function() {
      console.log("Aplicação iniciada!");
   }

   return {
      init
   }
})();

module.init();